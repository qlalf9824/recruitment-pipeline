# 주요 설계 결정 기록

## 작성 원칙

- 프로젝트의 구조, 동작, 품질, 유지보수에 의미 있는 영향을 주는 결정만 기록한다.
- 사소하거나 쉽게 되돌릴 수 있는 구현 선택은 강제로 기록하지 않는다.
- `REQUIREMENTS.md`는 무엇을 충족해야 하는지 정의하고, 이 문서는 그 요구사항을 충족하기 위해 무엇을 왜 선택했는지 기록한다.
- 요구사항 자체를 설계 결정처럼 다시 작성하지 않는다.
- 최종 선택뿐 아니라 선택 기준, 대안, Trade-off를 함께 기록한다.
- 결정하지 않았거나 확인하지 않은 내용은 추론하지 않고 placeholder로 남긴다.
- 새로운 결정이 필요할 때 문서 끝의 템플릿을 복사해 사용한다.

## 기록

### DEC-001: 요구사항 문서의 구성과 설계 경계

- 상태: 채택
- 결정일: 2026-08-31
- 관련 요구사항: `REQUIREMENTS.md` 전체
- 관련 결정: 없음

#### 1. 결정해야 했던 문제

초안은 기능 목록과 간단한 설명만 있어 구현 순서, 기능별 실패 상황, 검증 기준, 의존성을 빠르게 확인하기 어려웠다. 동시에 요구사항을 구체화하면서 원문에 없는 기술 구현까지 결정하지 않도록 문서의 경계를 정해야 했다.

#### 2. 고려한 선택지

| 선택지 | 기대 효과 | 제약·위험 |
| --- | --- | --- |
| 구현 순서에 따른 기능별 구조로 개편 | 개발 중 빠르게 참조하고 기능별 완료 여부를 검증하기 쉬움 | 공통 구조 반복으로 문서가 길어짐 |
| 사용자 흐름에 따라 구성 | 사용자가 보드를 이용하는 순서가 자연스럽게 드러남 | 실제 구현 선후관계가 상대적으로 덜 명확함 |
| 조회·표시·변경·품질 등 기능군으로 분리 | 기능 범주가 명확하게 구분됨 | 구현 중 빠르게 선후관계를 확인하기 어려움 |

#### 3. 최종 선택

필수 구현과 선택 구현을 분리하고, 각 기능을 실제 구현 순서에 따라 배치한다. 모든 기능은 목적, 요구 동작, 예외·실패 상황, Acceptance Criteria, 의존성으로 구성한다. 요구되는 동작과 검증 결과만 명시하고, 별도로 결정되지 않은 기술 구현은 포함하지 않는다.

#### 4. 선택 이유

- 사용자가 구현 순서, 기능별 공통 구조, 의존성, 검증 가능한 Acceptance Criteria를 명시적으로 요구했다.
- 필수 기능과 선택 기능을 분리하면 최소 완료 범위와 추가 품질 범위를 구분할 수 있다.
- 구현 방식을 제외하면 이후 설계 단계에서 대안을 비교할 여지를 유지할 수 있다.
- 반복되는 공통 구조가 AI와 사용자의 기능별 완료 여부 확인에 적합하다.

#### 5. Trade-off / 포기한 점

- 같은 소제목이 반복되어 기존 목록형 초안보다 문서가 길어졌다.
- 라이브러리, API 구조, 상태 관리, 단계 이동 조작 방식은 문서만으로 결정할 수 없다.
- 구현을 시작하기 전에 요구사항과 별도로 필요한 설계 결정을 추가해야 한다.

#### 6. AI 제안 검토

| AI 제안 내용 | 처리 결과 | 이유 |
| --- | --- | --- |
| 구현 순서형 문서 구성을 사용 | 채택 | 사용자가 개발 중 빠르게 참고하고 기능 의존성을 확인하려는 목적에 가장 부합함 |
| 사용자 흐름형 또는 기능군 분리형 구성 | 기각 | 요구사항에서 가장 우선한 실제 구현 순서를 상대적으로 명확하게 보여주지 못함 |
| 정해지지 않은 UI와 API 구현 방식을 요구사항에서 제외 | 수정 후 채택 | 원칙은 유지하되, 사용자가 명시한 상세 보기 형태는 모달 또는 사이드바로 범위를 제한함 |

### DEC-002: 이름 검색과 직무 필터의 결합 규칙

- 상태: 채택
- 결정일: 2026-08-31
- 관련 요구사항: `2.5 이름 검색과 직무 필터`
- 관련 결정: 없음

#### 1. 결정해야 했던 문제

초안에는 이름 검색과 직무별 필터만 명시되어 있어 이름의 일치 방식, 직무 선택 개수, 두 조건의 결합 방식이 정해지지 않았다. 같은 입력에 항상 같은 결과를 기대하고 검증하려면 각 조건의 동작을 결정해야 했다.

#### 2. 고려한 선택지

| 선택지 | 기대 효과 | 제약·위험 |
| --- | --- | --- |
| 직무 한 개만 선택 | 한 직무의 지원자만 확인 가능 | 여러 직무의 지원자를 한 번에 볼 수 없음 |
| 직무 여러 개 선택 | 선택한 여러 직무의 지원자를 한 번에 확인 가능 | 선택한 직무 사이의 결합 규칙을 추가로 정해야 함 |
| 이름 부분 일치 및 영문 대소문자 미구분 | 이름 전체와 대소문자를 정확히 입력하지 않아도 검색 가능 | 하나의 검색어에 여러 이름이 일치할 수 있음 |
| 이름 검색과 직무 필터를 AND로 결합 | 두 조건을 모두 만족하는 지원자만 확인 가능 | 한 조건만 만족하는 지원자는 결과에서 제외됨 |

#### 3. 최종 선택

이름은 부분 일치로 검색하고 영문 대소문자를 구분하지 않는다. 직무는 여러 개를 선택할 수 있으며, 선택한 직무 중 하나에 해당하면 직무 조건을 만족한다. 이름 검색과 직무 필터를 함께 적용하면 두 조건을 모두 만족하는 지원자만 표시한다.

#### 4. 선택 이유

- 사용자는 이름 검색의 편의를 위해 부분 일치와 영문 대소문자 미구분을 선택했다.
- 사용자는 여러 직무의 지원자를 한 번에 보는 것이 편리하다는 이유로 다중 선택을 선택했다.
- 사용자는 이름 검색과 직무 필터를 함께 사용할 때 AND 조건으로 적용하도록 확인했다.

#### 5. Trade-off / 포기한 점

- 부분 검색은 정확히 일치하는 이름만 표시하는 방식보다 결과 범위가 넓어질 수 있다.
- AND 결합에서는 이름 또는 직무 중 하나의 조건만 만족하는 지원자를 함께 볼 수 없다.

#### 6. AI 제안 검토

| AI 제안 내용 | 처리 결과 | 이유 |
| --- | --- | --- |
| 이름 검색과 직무 필터를 AND로 결합 | 채택 | 사용자가 해당 결합 규칙을 직접 확인함 |
| 이름은 부분 일치로 검색하고 영문 대소문자를 구분하지 않음 | 채택 | 사용자가 검색 편의성에 더 적합하다고 판단함 |
| 직무 필터를 단일 선택과 다중 선택 중에서 결정 | 수정 후 채택 | 사용자가 여러 직무를 함께 보기 위해 다중 선택을 선택함 |

### DEC-003: 지원자 상세 보기의 정보와 동작 범위

- 상태: 채택
- 결정일: 2026-08-31
- 관련 요구사항: `2.6 지원자 상세 보기`
- 관련 결정: 없음

#### 1. 결정해야 했던 문제

초안에는 지원자 상세 보기만 명시되어 있어 표시할 정보, 정보 수정 가능 여부, 허용할 화면 형태가 정해지지 않았다. 상세 기능의 완료 범위를 검증할 수 있도록 이 세 가지를 구체화해야 했다.

#### 2. 고려한 선택지

| 선택지 | 기대 효과 | 제약·위험 |
| --- | --- | --- |
| API가 제공하는 상세 정보를 별도 범위 없이 표시 | 제공되는 정보를 폭넓게 확인 가능 | 표시 범위가 API 구성에 따라 달라질 수 있음 |
| 필요한 상세 필드를 명시 | 표시 범위와 완료 조건이 명확함 | 명시되지 않은 추가 정보는 표시 대상에 포함되지 않음 |
| 조회와 정보·메모 편집을 함께 제공 | 상세 화면에서 내용을 갱신 가능 | 사용자가 요구하지 않은 작성·수정 기능까지 범위가 확대됨 |
| 조회 전용으로 제공 | 상세 정보 확인에 범위를 집중할 수 있음 | 상세 화면에서 정보나 메모를 변경할 수 없음 |
| 상세 화면 형태를 제한하지 않음 | 구현 단계에서 화면 형태를 자유롭게 선택 가능 | 요구된 화면 경험의 범위가 명확하지 않음 |
| 모달 또는 사이드바로 제한 | 보드를 유지하면서 상세 정보를 표시하는 범위가 명확함 | 두 형태 중 하나를 선택하는 설계 판단은 이후에 필요함 |

#### 3. 최종 선택

상세 보기에는 이름, 직무, 지원일, 현재 단계, 이력 정보, 메모를 표시한다. 모든 정보는 조회 전용이며, 상세 보기는 모달 또는 사이드바 중 하나의 형태로 제공한다.

#### 4. 선택 이유

- 사용자가 카드와 상세 화면에 필요한 정보 항목을 직접 구분해 지정했다.
- 사용자가 이력 정보와 메모를 포함한 상세 정보를 조회만 가능하게 하도록 지정했다.
- 사용자가 상세 보기의 허용 형태를 모달 또는 사이드바로 명시했다.

#### 5. Trade-off / 포기한 점

- 상세 화면에서 지원자 정보, 이력 정보, 메모를 작성하거나 수정할 수 없다.
- 모달과 사이드바 중 어느 형태를 최종 선택할지는 이 결정에서 정하지 않았다.

#### 6. AI 제안 검토

| AI 제안 내용 | 처리 결과 | 이유 |
| --- | --- | --- |
| API가 제공하는 상세 정보를 누락 없이 표시하는 수준으로 정의 | 기각 | 사용자가 표시할 상세 필드를 직접 지정함 |
| 원문 범위에 따라 상세 정보를 조회 전용으로 정의 | 채택 | 사용자가 조회만 가능하도록 확인함 |
| 상세 보기의 화면 형태를 제한하지 않음 | 수정 후 채택 | 사용자가 모달 또는 사이드바로 허용 범위를 명시함 |

### DEC-004: 지원자 mock API의 계층과 주입 방식

- 상태: 채택
- 결정일: 2026-08-31
- 관련 요구사항: `2.1 지원자 데이터 조회와 초기 상태 처리`, `2.3 지원자 단계 이동과 저장`, `2.4 낙관적 업데이트와 실패 복구`, `3.4 핵심 동작 테스트`
- 관련 결정: 없음

#### 1. 결정해야 했던 문제

실제 백엔드 없이 지원자 목록 조회와 단계 변경을 제공하면서 새로고침 후 변경을 유지하고, 실패와 지연을 재현 가능하게 만들어야 했다. 동시에 `ApplicantApi`의 책임을 요청 검증, 데이터 조정, 오류 우선순위 결정으로 제한하고, localStorage 접근과 난수·타이머처럼 실행 환경에 따라 달라지는 외부 효과를 API 경계 밖에 둘 방법이 필요했다. 구체 구현을 직접 import하면 UI와 API orchestration이 브라우저 전역 및 무작위 동작에 결합되므로, 어떤 계약에 의존하고 어디에서 구현을 조립할지 결정해야 했다.

#### 2. 고려한 선택지

| 선택지 | 기대 효과 | 제약·위험 |
| --- | --- | --- |
| 앱 내부 TypeScript mock API | 요구 규모에 맞는 작은 구현과 직접적인 타입 재사용 | 실제 HTTP 요청·상태 코드 경계를 그대로 검증하지 못함 |
| MSW 기반 HTTP mock | fetch, URL, 메서드, 상태 코드 등 실제 API와 유사 | localStorage 계층은 별도로 필요하고 현재 범위에 설정 비용이 큼 |
| API가 storage와 behavior 구현을 직접 import | 파일 수와 조립 코드가 적고 호출 경로가 짧음 | localStorage, 난수, 타이머 획득 방식이 API에 고정되어 실패 재현과 구현 교체가 전역 mocking에 의존함 |
| storage와 behavior 계약을 API factory에 주입 | API가 호출 계약에만 의존하고 production·test에서 서로 다른 구현을 조립할 수 있음 | interface, factory, 앱 조립 코드와 명시적인 생명주기 관리가 추가됨 |
| 목록과 상세 필드를 하나의 Applicant에 포함 | 한 객체로 모든 화면 데이터 사용 가능 | 목록 저장소가 아직 구현하지 않는 상세 범위까지 소유함 |
| 목록 Applicant와 상세 모델을 분리 | 현재 API가 보드 필드에만 집중 | 상세 기능 구현 시 별도 모델과 API 계약이 필요함 |

#### 3. 최종 선택

앱 내부 TypeScript mock API를 사용한다. seed, localStorage 저장소, mock API 오류, 성공·실패·지연 behavior, Applicant API orchestration을 분리한다. `createApplicantApi`는 구체 구현을 import하지 않고 `ApplicantStorage`와 `MockApiBehaviorService` 계약을 주입받는다. 이로써 API는 지원자 조회·변경 호출 타입과 오류 규칙에만 의존한다.

`main.tsx`는 유일한 composition root로서 렌더 전에 localStorage 구현과 behavior 구현을 한 번 생성해 Applicant API에 주입한다. React Context는 서비스를 생성하지 않고 이미 조립된 인스턴스를 UI에 전달하며, UI는 `useApplicantApi`만 사용한다. production에서는 브라우저 storage와 기본 난수를, 테스트에서는 jsdom storage·in-memory storage·고정 behavior·주입 난수를 같은 계약 아래 사용할 수 있다.

`Applicant`에는 목록에 필요한 `id`, `name`, `position`, `appliedAt`, `stage`만 두며 상세 모델은 요구사항 `2.6` 구현 시 결정한다. 외부에서 값으로 사용하는 단계, 상태, 오류 코드, outcome, delay mode는 `as const` 객체에서 union을 파생한다.

저장 키가 없으면 seed 복사본을 반환하고 최초 성공 변경 시 전체 배열을 저장한다. 잘못된 stage는 400, 없는 ID는 404, 손상된 저장 데이터와 시뮬레이션·예상 밖 오류는 500으로 구분한다. 실패와 지연은 각각 독립적인 10% 확률이며 무작위 지연은 300~2,000ms다. 명시 설정과 주입된 난수 함수로 테스트를 결정론적으로 수행한다.

#### 4. 선택 이유

- 실제 백엔드는 요구 범위가 아니며 현재 데이터 동작은 목록 조회와 단계 변경으로 제한된다.
- localStorage 구현을 MSW 사용 여부와 관계없이 만들어야 하므로 HTTP mocking 추가 비용의 이점이 작다.
- `ApplicantApi`가 storage 획득과 지연 생성까지 담당하면 데이터 orchestration과 실행 환경 제어라는 서로 다른 책임이 한 모듈에 섞인다. 주입을 사용하면 API는 요청 검증, 데이터 선택·변경, 오류 변환 순서에만 집중한다.
- `ApplicantStorage` 주입은 정상 localStorage뿐 아니라 데이터 손상, 접근 차단, quota 실패를 같은 호출 계약으로 표현하게 한다. 브라우저 storage getter도 실제 `load`·`save` 시점에 평가하므로 부팅 실패가 아니라 API의 `INTERNAL_ERROR` 경계로 전달된다.
- `MockApiBehaviorService` 주입은 난수와 타이머를 API에서 제거한다. production의 10% 무작위 동작과 테스트의 고정 성공·실패·지연을 같은 API 흐름에 적용하므로 전역 `Math.random`이나 timer 구현을 교체하지 않고 오류 우선순위를 검증할 수 있다.
- `main.tsx`에 구체 구현 조립을 모으면 서비스 생성 시점과 생명주기가 한 곳에 드러난다. Context는 전달만 담당하므로 React 재렌더가 API 인스턴스를 다시 만들지 않고 UI도 구현 계층을 import하지 않는다.
- 목록 모델에서 상세 필드를 제거하면 아직 정하지 않은 상세 API 구조가 현재 저장 계약에 고정되지 않는다.
- Vitest, jsdom, React Testing Library로 각 계층과 Context 경계를 자동 검증할 수 있다.

#### 5. Trade-off / 포기한 점

- 실제 HTTP 요청, 직렬화, URL, 메서드 계약은 검증하지 않는다.
- 단순 직접 import 방식보다 interface, factory, Context 파일이 늘어나고 의존성을 전달하는 코드가 필요하다.
- 구체 구현 생성과 서비스 생명주기를 `main.tsx`에서 명시적으로 관리해야 한다.
- 작은 애플리케이션에서는 주입 구조가 직접 import보다 장황하지만, 이번 요구사항처럼 storage 실패와 무작위·지연을 독립적으로 제어해야 하는 경우에는 분리 비용을 감수한다.
- 상세 기능을 구현할 때 별도의 상세 모델과 조회 계약을 추가 설계해야 한다.

#### 6. AI 제안 검토

| AI 제안 내용 | 처리 결과 | 이유 |
| --- | --- | --- |
| MSW 대신 앱 내부 TypeScript mock API와 localStorage 계층 사용 | 채택 | 현재 요구 규모에서는 HTTP mock 설정 비용이 더 크다고 사용자가 판단함 |
| storage와 behavior를 모두 Applicant API에 주입 | 채택 | API를 호출 계약과 오류 orchestration에만 의존시키고 브라우저 storage·난수·타이머를 교체 가능한 외부 효과로 분리하려는 사용자 의도에 부합함 |
| 상세 필드를 Applicant에 유지 | 기각 | 사용자가 상세 모델을 이후 별도 정의하도록 요청함 |
| 공개 값 타입을 const 객체에서 파생 | 채택 | 기존 단계 상수 패턴과 같은 재사용·타입 안전성을 원함 |
| API 인스턴스를 앱 시작 시 조립하고 Context로 제공 | 채택 | 구체 구현과 서비스 생명주기를 composition root 한 곳에서 관리하고 Context는 생성이 아닌 전달만 담당하게 함 |

### DEC-005: 지원자 조회 상태를 TanStack React Query로 관리

- 상태: 채택
- 결정일: 2026-08-31
- 관련 요구사항: `2.1 지원자 데이터 조회와 초기 상태 처리`
- 관련 결정: `DEC-004`

#### 1. 결정해야 했던 문제

주입된 `ApplicantApi`로 화면 진입 시 목록을 조회하면서 로딩, 오류, 빈 결과, 성공 상태와 명시적 재시도를 일관되게 관리해야 했다. 브라우저 이벤트나 라이브러리 기본 재시도가 mock API의 무작위 실패·지연과 겹쳐 요청 횟수를 예측하기 어려워지지 않도록 조회 정책도 정해야 했다.

#### 2. 고려한 선택지

| 선택지 | 기대 효과 | 제약·위험 |
| --- | --- | --- |
| 컴포넌트의 로컬 상태로 직접 관리 | 추가 의존성 없이 구현 가능 | 요청 상태, 캐시, 재조회 흐름을 화면마다 직접 관리해야 함 |
| TanStack React Query hook으로 관리 | 비동기 조회 상태와 명시적 재조회를 표준화 | 의존성과 QueryClient Provider가 추가되고 기본 자동 동작을 요구사항에 맞게 설정해야 함 |

#### 3. 최종 선택

`useApplicantQuery`가 Context에서 `ApplicantApi`를 받아 TanStack React Query v5의 `useQuery`로 조회한다. `QueryClient`는 `main.tsx`에서 한 번 생성하며, 자동 retry와 window focus·network reconnect refetch는 비활성화한다. `App`은 query 결과를 로딩, 오류, 콘텐츠 컴포넌트로 분기하고 오류 상태에서는 명시적 재시도만 허용한다. 성공 응답은 0건도 콘텐츠로 전달하며 전체·컬럼 빈 상태는 후속 보드가 처리한다.

#### 4. 선택 이유

- API 객체 생성과 저장소 접근은 기존 Context 경계에 남기고 hook은 호출 계약만 소비할 수 있다.
- 요청 상태를 React Query에 맡겨 UI가 별도의 데이터·오류 상태를 중복 보관하지 않는다.
- 자동 요청을 비활성화하면 한 번의 재시도 동작이 정확히 한 번의 새 API 호출로 이어진다.
- 조회 상태별 표시를 독립 컴포넌트로 두고 성공 데이터 표현은 보드가 확장할 콘텐츠 경계에 모을 수 있다.

#### 5. Trade-off / 포기한 점

- 현재 단일 조회 화면에 QueryClient와 외부 의존성이 추가된다.
- 캐시의 자동 재검증보다 예측 가능한 초기 조회와 사용자 재시도 흐름을 우선한다.
- React Query v5는 오류 후 명시적 refetch 중 다시 pending 상태가 되므로 최초 로딩과 재시도를 `isFetchedAfterMount`로 구분해야 한다.

#### 6. AI 제안 검토

| AI 제안 내용 | 처리 결과 | 이유 |
| --- | --- | --- |
| `useApplicantQuery`를 `src/hooks/`에 두고 API Context를 소비 | 채택 | UI와 API 구현 계층의 분리를 유지함 |
| 로딩·오류·빈 결과·콘텐츠 컴포넌트 분리 | 수정 후 채택 | 독립 빈 결과 컴포넌트는 제거하고 보드가 전체·컬럼 빈 상태를 처리하도록 성공 데이터를 콘텐츠에 통합함 |
| 자동 retry, focus refetch, reconnect refetch 비활성화 | 채택 | 명시하지 않은 추가 요청을 막고 재시도 횟수를 보장함 |
| 재시도 중 오류 화면과 비활성 버튼 유지 | 수정 후 채택 | 실제 v5 상태 전이를 확인해 `isFetchedAfterMount` 분기를 추가함 |

---

### DEC-006: Tailwind v4 기반 보드 우선 스타일 전환

- 상태: 채택
- 결정일: 2026-08-31
- 관련 요구사항: `2.2 단계별 보드와 지원자 카드 표시`
- 관련 결정: `DEC-005`

#### 1. 결정해야 했던 문제

기존 지원자 보드는 컴포넌트 전용 plain CSS로 스타일을 보유하고 있었다. 개발 속도와 UI 일관성을 높이기 위해 Tailwind를 도입하되, 전체 UI를 한 번에 바꾸지 않고 현재 완료된 보드부터 전환하면서 유틸리티 감지 방식과 전역 스타일 경계를 정해야 했다.

#### 2. 고려한 선택지

| 선택지 | 기대 효과 | 제약·위험 |
| --- | --- | --- |
| plain component CSS 유지 | JSX가 간결하고 컴포넌트별 스타일 파일을 분리할 수 있음 | 반복되는 값과 명명 규칙이 늘고 화면 간 일관성을 수동으로 유지해야 함 |
| Tailwind utility를 JSX에 직접 사용 | 표준화된 spacing·color·typography utility로 구현 속도와 일관성을 높이고 사용 위치를 JSX에서 바로 확인 가능 | JSX의 class 문자열이 길어지고 Tailwind build 의존성이 추가됨 |
| `@apply`로 기존 component CSS class 유지 | 기존 class 이름을 유지하면서 utility 토큰을 재사용 가능 | board 전용 class 계층을 다시 만들고 JSX와 CSS를 함께 추적해야 하며 utility-first 전환의 이점이 줄어듦 |

#### 3. 최종 선택

공식 `@tailwindcss/vite` plugin을 사용하는 Tailwind CSS v4를 채택한다. `src/styles.css`는 Tailwind import와 최소 전역 base 선언만 보유하며, 보드의 레이아웃·컬럼·카드·빈 상태는 JSX의 utility로 표현한다. `@apply`로 board class를 재생성하지 않고 `ApplicantBoard.css`는 제거한다.

전체 UI 전환의 첫 범위는 현재 지원자 보드로 제한한다. 로딩과 오류 컴포넌트의 마크업·스타일은 이번 전환에 포함하지 않는다. 고정 다섯 단계의 색상 utility는 `applicantBoardStages.ts`에 완전한 정적 문자열로 선언하고, stage 값으로 class 조각을 동적으로 만들지 않는다.

#### 4. 선택 이유

- 공식 Vite plugin은 기존 Vite pipeline에 Tailwind v4 compilation을 직접 연결한다.
- 보드 우선 전환은 사용자에게 보이는 완료 화면을 먼저 일관되게 만들면서 로딩·오류 상태의 동작 범위를 불필요하게 넓히지 않는다.
- JSX의 utility는 spacing, typography, border, surface token을 가까이 두어 반복 화면을 더 빠르게 만들고 일관성을 유지하게 한다.
- 정적 단계 문자열은 Tailwind가 build 시 모든 상태별 utility를 확실히 생성하게 하며, 고정 stage 표현을 한 구성 파일에서 관리하게 한다.

#### 5. Trade-off / 포기한 점

- utility가 많은 JSX는 plain CSS selector보다 길고 시각적 밀도가 높다.
- Tailwind와 Vite plugin이라는 새 build dependency 및 업그레이드 대상이 추가된다.
- loading·error 컴포넌트의 Tailwind 전환과 전체 UI 통일은 후속 작업으로 남는다.

#### 6. AI 제안 검토

| AI 제안 내용 | 처리 결과 | 이유 |
| --- | --- | --- |
| plain CSS, direct utility, `@apply` 유지 방식을 비교 | 채택 | 스타일 시스템 전환의 유지보수·일관성·JSX 복잡도 trade-off를 명확히 하기 위해 필요함 |
| 공식 Tailwind v4 Vite plugin으로 build pipeline 구성 | 채택 | 별도 PostCSS 또는 v3 config 없이 기존 Vite에 맞는 공식 통합 방식임 |
| 전체 전환 전에 현재 보드를 첫 범위로 전환 | 채택 | 사용자가 전체 전환의 시작 범위를 현재 보드로 지정함 |
| stage별 class를 동적으로 조합 | 기각 | Tailwind utility 감지를 보장하기 위해 complete static string이 필요함 |

---

## 새 결정 템플릿

### DEC-<!-- 번호 -->: <!-- 결정 제목 -->

- 상태: <!-- 검토 중 / 채택 / 대체됨 / 철회 -->
- 결정일: <!-- YYYY-MM-DD -->
- 관련 요구사항: <!-- REQUIREMENTS.md의 항목 번호 또는 제목. 관련 항목이 없으면 없음. -->
- 관련 결정: <!-- 선행·후속 결정 번호. 관련 결정이 없으면 없음. -->

#### 1. 결정해야 했던 문제

<!-- 어떤 문제가 있었고, 왜 프로젝트 차원의 결정이 필요했는지 기록한다. 요구사항 자체가 아니라 선택이 필요한 지점을 설명한다. -->

#### 2. 고려한 선택지

| 선택지 | 기대 효과 | 제약·위험 |
| --- | --- | --- |
| <!-- 선택지 --> | <!-- 장점 또는 기대 효과 --> | <!-- 단점, 제약 또는 위험 --> |

#### 3. 최종 선택

<!-- 최종적으로 선택한 내용을 구체적으로 기록한다. 아직 결정하지 않았다면 내용을 작성하지 않는다. -->

#### 4. 선택 이유

- <!-- 요구사항, 제약, 검증 결과 등 선택의 근거를 기록한다. -->

#### 5. Trade-off / 포기한 점

- <!-- 최종 선택으로 얻지 못하게 된 장점, 감수한 비용이나 위험을 기록한다. -->

#### 6. AI 제안 검토

| AI 제안 내용 | 처리 결과 | 이유 |
| --- | --- | --- |
| <!-- 실제로 받은 주요 제안. 제안이 없으면 없음. --> | <!-- 채택 / 수정 / 기각 --> | <!-- 처리 결과를 결정한 이유 --> |
