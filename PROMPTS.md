# AI 협업 기록

## 작성 원칙

- 기능 하나의 작업 과정은 하나의 섹션에 기록하고, 가능하면 커밋 하나와 대응시킨다.
- 구현 방향이나 결과에 영향을 준 주요 프롬프트만 기록한다.
- 사소한 오타 수정, 단순 확인 질문, 반복 요청은 생략할 수 있다.
- AI의 제안과 직접 확인한 사실을 구분하고, 검증하지 않은 내용을 검증한 것처럼 기록하지 않는다.
- 수행하지 않은 항목은 내용을 만들지 않고 placeholder로 남긴다.
- 기능별 섹션이 필요할 때 문서 끝의 템플릿을 복사해 사용한다.

## 기록

### 요구사항 구체화

- 관련 요구사항: `REQUIREMENTS.md` 전체
- 작업 상태: 완료
- 관련 커밋: 이 문서를 포함한 커밋

#### 주요 프롬프트

> 채용 파이프라인 보드 웹 페이지를 개발하려고 해 React, typescript, vite 프로젝트만 생성되어있고 아직 아무 기능도 구현되어 있지 않아.
>
> [REQUIREMENTS.md](REQUIREMENTS.md)를 초안을 작성해두었는데 이걸 기반으로 구체화 시키려고 해.
>
> 구현 코드나 구체적인 기술 설계를 작성하지 말아줘.
>
> 다음 원칙을 따라줘.
>
> 1. 요구사항을 실제 구현 순서에 맞춰 기능별로 정리한다.
> 2. 각 기능은 아래 구조로 작성한다.
>    - 목적
>    - 요구 동작
>    - 예외 / 실패 상황
>    - Acceptance Criteria
> 3. 기능 간 의존성이 있다면 명시한다.
> 4. 원문에 없는 구현 방법은 임의로 결정하지 않는다.
> 5. 구현 방법이 여러 가지인 경우 특정 방법을 선택하지 말고 요구되는 동작만 명시한다.
> 6. AI가 구현 여부를 체크할 수 있도록 Acceptance Criteria는 구체적이고 검증 가능한 문장으로 작성한다.
> 7. 설명은 짧고 명확하게 작성하고, 개발 중 빠르게 참고할 수 있는 형태를 우선한다.

주요 후속 프롬프트:

- “선택 구현 항목으로 구분해 정리해줘.”
- “예외 상황이나 불합격으로 바로 넘길 수 있도록 하기 위해 모든 다른 단계로 이동할 수 있게 해줘.”
- “응 AND 조건으로 검색하도록 해줘.”
- “여러 직무를 한번에 볼 수 있도록 하는 것이 더 편할 거 같아서 다중 선택으로 가면 좋을 거 같아.”
- “응 부분 검색, 대소문자는 구분하지 않는게 검색 편의성에 더 좋을 거 같아.”
- “카드 - 이름, 직무, 지원일, 현재 단계 표시 / 상세 - 이름, 직무, 지원일, 현재 단계, 이력 정보, 메모. 위 정보로 보이도록 해줘.”
- “응 재시도 할 수 있게 버튼을 추가해주면 좋을 거 같아.”
- “응 사용자가 마지막으로 선택한 단계가 결국 변경하고 싶은 단계일거라 마지막으로 선택한 단계가 최종 상태가 되는 것이 좋을 거 같아.”
- “새로고침 후에 유지되는 거 까지는 필요없을 거 같아 사용자가 취소를 하고 싶을 때는 방금 한 행동에 대한 걸 취소하고 싶을테니까.”
- “응 그렇게 명시해줘.” — 1,000건 기준 검색·필터 결과의 200ms 이내 반영 기준에 대한 확인
- “조회만 가능하도록 작업해줘.”
- “상세 보기는 모달이나 사이드바 형태로 구현한다고 작성해줘.”

#### AI가 제안한 핵심 내용

- 필수 구현과 선택 구현을 분리하고, 각 영역을 실제 구현 순서에 맞춰 배치한다.
- 모든 기능에 목적, 요구 동작, 예외·실패 상황, Acceptance Criteria, 의존성을 동일하게 적용한다.
- 데이터 조회와 상태 처리부터 보드 표시, 단계 이동, 낙관적 업데이트, 검색·필터, 상세 보기 순으로 구성한다.
- 원문이나 후속 답변에서 정해지지 않은 라이브러리, API 구조, 단계 이동 조작 방식은 요구사항에서 결정하지 않는다.
- 성능, 경쟁 상태, 롤백처럼 모호할 수 있는 항목은 관찰 가능한 결과와 수치로 검증 기준을 작성한다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상        | 확인 방법 또는 근거                                            | 결과                                                          |
| --------------------- | -------------------------------------------------------------- | ------------------------------------------------------------- |
| 필수·선택 범위        | 도전 요구사항을 선택 구현으로 분리하도록 직접 지정             | 필수 6개 기능과 선택 5개 기능으로 분리                        |
| 단계 이동 범위        | 예외 처리와 불합격 전환을 고려해 모든 단계 간 이동을 직접 선택 | 현재 단계를 제외한 네 단계로 이동 가능하도록 반영             |
| 검색·필터 조건        | AND 결합, 다중 직무, 부분 검색, 대소문자 미구분을 각각 확인    | 요구 동작과 Acceptance Criteria에 반영                        |
| 상세 정보 범위        | 표시 필드와 조회 전용 여부를 직접 지정                         | 6개 상세 항목을 조회 전용으로 반영                            |
| 실패 대응             | 초기 조회 실패 시 재시도 필요 여부를 직접 확인                 | 오류 안내와 재시도 동작을 반영                                |
| 선택 기능의 기대 결과 | 연속 이동, Undo 유지 범위, 1,000건 성능 기준을 직접 확인       | 최신 이동 우선, 새로고침 시 Undo 이력 제거, 200ms 기준을 반영 |
| 상세 보기 형태        | 작성된 요구사항의 화면 형태 범위를 추가로 검토                 | 모달 또는 사이드바로 범위를 수정                              |

#### AI 제안에서 발견한 문제

- 최초 작성본은 상세 보기의 화면 형태를 제한하지 않았으나, 이후 사용자가 모달 또는 사이드바로 범위를 명확히 지정했다. 해당 문구를 그대로 두면 요구사항과 충돌하므로 수정이 필요했다.

#### 최종 결정

- 결정: 수정 후 채택
- 반영 내용: 필수·선택 기능 분리, 기능별 공통 구조, 구현 순서, 의존성, 검증 가능한 Acceptance Criteria를 채택했다. 사용자 확인을 거친 단계 이동, 검색·필터, 상세 정보, 실패 대응, 경쟁 상태, Undo, 성능 조건을 반영하고 상세 보기 형태는 모달 또는 사이드바로 수정했다.
- 결정 근거: 각 항목을 대화에서 직접 확인했으며, 정하지 않은 구체적인 기술 구현은 요구사항 문서에서 제외했다.

---

### 지원자 데이터 모델 정의

- 관련 요구사항: `2.2 단계별 보드와 지원자 카드 표시`, `2.3 지원자 단계 이동과 저장`, `2.4 낙관적 업데이트와 실패 복구`, `2.5 이름 검색과 직무 필터`, `2.6 지원자 상세 보기`
- 작업 상태: 완료
- 관련 커밋: `2f7361e`

#### 주요 프롬프트

> `REQUIREMENTS.md`를 바탕으로 지원자 데이터 구조 초안을 검토해줘. 필수 요구사항 구현에 부족하거나 불필요한 필드, 단계 이동 시의 문제, 200~1,000건 데이터에서의 구조적 문제를 확인해줘.

주요 후속 프롬프트:

- “의미를 명확하게 가져가기 위해서 `documentReview`로 해주고 이력 정보는 자율성을 위해 `string | null`로 하는 게 좋을 거 같아.”
- “`APPLICANT_STAGES`는 `as const` 기반 union type으로 작성해줘.”
- “단계 상수는 객체 형태로 만들고 `(typeof APPLICANT_STAGE)[keyof typeof APPLICANT_STAGE]`로 union type을 만들어줘.”

#### AI가 제안한 핵심 내용

- 카드 표시, 검색, 단계 이동에 필요한 초안 필드는 유지하고, 상세 보기에 필요한 `resume`과 `memo`를 추가한다.
- 이력 정보와 메모가 없는 상태를 명확히 표현하도록 두 필드를 `string | null`로 정의한다.
- 단계 값은 일반 `string` 대신 고정된 문자열 union으로 제한한다.
- 지원자 데이터는 단일 목록을 원본 상태로 두고 `stage`를 기준으로 컬럼을 계산하면 단계 이동 시 중복이나 누락 위험을 줄일 수 있다.
- 200~1,000건 규모에서는 평면적인 지원자 모델 자체가 검색·필터 성능의 병목이 되지 않는다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상     | 확인 방법 또는 근거                                             | 결과                                                                    |
| ------------------ | --------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 필수 필드          | `REQUIREMENTS.md` 2.2와 2.6의 카드·상세 표시 항목 대조          | `id`, `name`, `position`, `appliedAt`, `stage`, `resume`, `memo`로 정의 |
| 단계 표현          | 사용자가 객체형 상수와 `keyof typeof` 파생 방식을 직접 지정     | `APPLICANT_STAGE` 객체의 값에서 `ApplicantStage` union을 파생           |
| nullable 상세 정보 | 이력 정보와 메모가 없는 경우를 표시해야 하는 요구사항 확인      | `resume`과 `memo`를 문자열 또는 `null`로 정의                           |
| 타입 계약          | 구현 전 타입 검증 실패를 확인한 뒤 구현 후 TypeScript 빌드 수행 | 허용되지 않은 단계 값은 거부되고 정의한 모델은 컴파일됨                 |
| 정적 검증          | `npm run build`, `npm run lint` 실행                            | 두 명령 모두 성공                                                       |

#### AI 제안에서 발견한 문제

- 최초 구현은 단계 값을 배열 상수 `APPLICANT_STAGES`로 정의했지만, 사용자가 각 단계를 이름 있는 프로퍼티로 재사용할 수 있도록 객체 상수 `APPLICANT_STAGE` 형태를 요구했다.

#### 최종 결정

- 결정: 수정 후 채택
- 반영 내용: `APPLICANT_STAGE`를 `as const` 객체로 정의하고 `(typeof APPLICANT_STAGE)[keyof typeof APPLICANT_STAGE]`에서 `ApplicantStage`를 파생했다. `Applicant`의 `resume`과 `memo`는 `string | null`로 정의했다.
- 결정 근거: 고정 단계의 타입 안전성을 유지하면서도 UI와 단계 이동 코드에서 의미 있는 상수 프로퍼티를 직접 재사용할 수 있고, 상세 정보가 없는 상태를 명시적으로 표현할 수 있다.

---

### 지원자 mock API와 localStorage 계층 구현

- 관련 요구사항: `2.1 지원자 데이터 조회와 초기 상태 처리`, `2.3 지원자 단계 이동과 저장`, `2.4 낙관적 업데이트와 실패 복구`, `3.4 핵심 동작 테스트`
- 작업 상태: 완료
- 관련 커밋: 미커밋

#### 주요 프롬프트

> `applicant.ts`를 바탕으로 mock API를 구현하려고 하며, 앱 내부 TypeScript mock API와 MSW를 구현 복잡도, 실제 API 유사성, local persistence, 실패·지연 테스트, 요구사항 대비 적정성으로 비교해줘.

주요 후속 프롬프트:

- “앱 내부 TypeScript mock API + 분리된 localStorage 저장소를 사용하는 방식이 더 적합한 거 같아.”
- “로컬 스토리지 관리 파일, api 파일 분리해서 생성하고 api 함수가 로컬 스토리지에 접근해서 데이터를 가져오고 업데이트 하는 방식으로 하면 좋겠어. 다른 UI 컴포넌트들은 로컬 스토리지 함수를 사용하지 않으면 좋겠어.”
- “저장데이터 손상은 500 입력 오류는 400으로 해줘.”
- “최초 조회에서는 seed 복사본만 반환하고 데이터 변경이 처음 성공할 때 localStorage에 전체 배열을 저장”하는 방식 승인
- 실패와 지연을 독립적으로 조합하고 각각 10%, 무작위 지연은 300~2,000ms로 지정
- “`Applicant`에서 상세는 따로 정의하게 `resume`, `memo`는 제거해주면 좋겠어.”
- “외부에서 상수를 사용하는 타입들은 union type으로 상수 객체로 만들어주면 좋겠어.”
- behavior 함수를 별도 파일로 분리하고 storage와 behavior를 Applicant API에 주입하도록 지정
- 웹 사이트 시작 시 Applicant API를 한 번 생성하고 Context API로 사용하도록 지정
- “분리가 정확히 되고 Applicant API는 호출 타입에 의존적으로 되도록 만들 수 있어서 좋을 것 같다”는 이유로 구체 구현 대신 계약에 의존하는 구조를 요청
- Vitest, jsdom, React Testing Library 도입 승인

#### AI가 제안한 핵심 내용

- 현재 규모에서는 MSW보다 앱 내부 TypeScript API와 분리된 localStorage 계층이 적합하다.
- seed, storage, API 오류, 성공·실패·지연 behavior, Applicant API orchestration, React Context를 각각 명확한 책임으로 분리한다.
- `ApplicantApi`의 책임을 요청 검증, 데이터 조정, 오류 우선순위 결정으로 제한하고 localStorage, 난수, 타이머 같은 외부 효과는 별도 구현으로 분리한다.
- `createApplicantApi`가 구체 storage·behavior 모듈을 직접 획득하지 않고 `ApplicantStorage`와 `MockApiBehaviorService` 계약을 주입받도록 한다. production과 test가 서로 다른 구현을 사용해도 API 호출 흐름은 유지된다.
- 공개 값 타입은 `as const` 객체의 값에서 union을 파생한다.
- `main.tsx`를 composition root로 사용해 구체 구현 생성과 API 생명주기를 한 곳에 모은다. Context는 서비스를 생성하지 않고 조립된 인스턴스를 전달하며 UI는 `useApplicantApi`로만 API에 접근한다.
- 테스트에서는 명시 설정, fake storage/behavior, 주입된 난수 함수와 fake timer를 사용해 무작위 실패 여부에 의존하지 않는다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상        | 확인 방법 또는 근거                                                 | 결과                                                                                                       |
| --------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 모델 범위             | `Applicant`와 seed 및 저장 검증 필드를 직접 확인                    | `resume`, `memo`를 제거하고 다섯 개 보드 필드만 유지                                                       |
| seed와 persistence    | seed 복사 격리, 초기 미저장, 최초 변경 전체 저장 테스트             | 요구한 저장 시점과 복사 경계 통과                                                                          |
| 저장 데이터 검증      | 잘못된 JSON·구조·단계·중복 ID·추가 상세 필드 테스트                 | 손상 데이터 보존 및 storage 전용 오류 확인                                                                 |
| 오류 우선순위         | 400, 404, storage 500, simulated 500, internal 500 테스트           | 합의한 상태와 저장 실패 시 미변경 확인                                                                     |
| 실패·지연             | 10% 경계, 300/2,000ms 경계, 독립 조합, 명시 설정 테스트             | 전역 난수에 의존하지 않고 결정론적으로 통과                                                                |
| 의존성 주입           | in-memory storage와 고정 behavior를 같은 계약으로 주입해 API 테스트 | API가 localStorage, timer, `Math.random`을 직접 사용하지 않고 요청 검증·데이터 변경·오류 우선순위만 조정함 |
| 구현 교체             | production graph와 jsdom 기반 재생성 graph를 각각 조립해 조회·변경  | 동일한 Applicant API 호출 타입을 유지하면서 browser storage와 test storage를 교체 가능                     |
| Context 경계          | React Testing Library로 동일 인스턴스와 Provider 누락 검증          | UI 소비 계약 확인                                                                                          |
| 전체 테스트           | `npm test`                                                          | 7개 파일, 59개 테스트 통과                                                                                 |
| 정적 검사             | `npm run lint`                                                      | 오류 없이 통과                                                                                             |
| 타입·production build | `npm run build`                                                     | TypeScript와 Vite build 통과                                                                               |
| 공백 검사             | `git diff --check`                                                  | 오류 없이 통과                                                                                             |

#### AI 제안에서 발견한 문제

- 최초 계획은 `ApplicantApiContext.tsx`에서 Provider와 hook을 함께 export했으나 ESLint Fast Refresh 규칙에 위배됐다. 규칙을 우회하지 않고 Context, Provider, hook을 각각 파일로 분리했다.
- Node 26의 실험적 전역 `localStorage`가 값 `undefined`로 이름을 선점해 Vitest가 jsdom storage를 전역에 복사하지 못했다. 저장소가 `Storage`를 주입받도록 설계되어 있었으므로 테스트에서 `jsdom.window.localStorage`를 명시적으로 전달해 실제 jsdom storage 동작을 검증했다.
- 코드 리뷰에서 `main.tsx`의 `window.localStorage` 즉시 접근이 브라우저 정책에 따라 앱 부팅을 중단할 수 있음을 확인했다. 저장소 factory가 `() => Storage`를 받고 실제 API 연산 시점에만 평가하도록 수정해 해당 오류도 API의 500 경계에서 처리되게 했다.

#### 최종 결정

- 결정: 수정 후 채택
- 반영 내용: 내부 TypeScript mock API, 변경 시점 localStorage 저장, const 기반 공개 union, storage·behavior 주입형 Applicant API, 앱 시작 지점의 단일 조립, Context 소비 구조를 구현했다. 주입을 통해 API는 구체 browser storage·난수·타이머가 아니라 조회·변경 호출 계약에 의존하고, `main.tsx`만 production 구현을 조립하도록 했다. `Applicant`의 상세 필드는 제거하고 상세 모델은 이후 작업으로 미뤘다. 로딩·빈 상태·오류·재시도와 보드 이동 같은 UI Acceptance Criteria는 후속 UI 작업 범위로 남아 있다.
- 결정 근거: 사용자가 구체 구현과 API orchestration을 분리해 호출 타입에 의존하도록 요청했다. 실제 검증에서 in-memory storage, 고정 behavior, 주입 난수, jsdom storage를 같은 계약으로 교체해 오류 우선순위와 persistence를 확인했으며, 자동 테스트와 lint·build·공백 검사를 통과했다.

### 지원자 조회 상태와 재시도 UI 구현

- 관련 요구사항: `2.1 지원자 데이터 조회와 초기 상태 처리`
- 작업 상태: 완료
- 관련 커밋: 미커밋

#### 주요 프롬프트

> react-query 를 통해 api 호출해서 로딩, 에러 상태 관리하면 좋을 거 같아

주요 후속 프롬프트:

- “`useApplicantQuery` 를 생성하는거로 하되 `hooks/` 폴더에 생성해줘.”
- “`ErrorComponent`, `NotFoundComponent`, `ContentComponent` 각각 따로 만들어서 api 상태에 따로 보여주도록 하자.”
- “응 로딩 컴포넌트도 추가해줘.”
- “not found 컴포넌트는 제거하자 보드 내에서 처리하는게 더 나을 거 같아.”

#### AI가 제안한 핵심 내용

- 앱 시작 시 하나의 `QueryClient`를 생성해 기존 Applicant API Provider 바깥에 제공한다.
- `useApplicantQuery`만 Context의 Applicant API와 React Query를 연결한다.
- 자동 retry와 focus·reconnect refetch를 끄고 오류 화면의 버튼으로만 다시 조회한다.
- 현재 `2.1` 범위의 성공 화면은 지원자 수만 표시하고 보드 컬럼과 카드는 후속 범위로 남긴다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상        | 확인 방법 또는 근거                                        | 결과                                                                   |
| --------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------- |
| 초기 상태 분기        | 미완료 promise, 1건, 0건, reject 응답으로 UI 테스트        | 로딩, 1건·0건 콘텐츠, 안전한 오류 안내 표시                            |
| 재시도                | 순차 응답과 deferred promise로 호출 수·상태 전환 검증      | 클릭 한 번당 한 번 호출, 성공 전환, 재실패 유지, 진행 중 버튼 비활성화 |
| 자동 조회 방지        | focus와 online 이벤트 발생 후 호출 수 확인                 | 추가 호출 없음                                                         |
| Context 경계          | 실제 QueryClientProvider와 ApplicantApiProvider로 App 렌더 | 주입된 Applicant API 결과 사용                                         |
| 전체 테스트           | `npm test`                                                 | 8개 파일, 67개 테스트 통과                                             |
| 정적 검사             | `npm run lint`                                             | 오류 없이 통과                                                         |
| 타입·production build | `npm run build`                                            | TypeScript와 Vite build 통과                                           |

#### AI 제안에서 발견한 문제

- React Query v5는 오류 후 `refetch()`가 진행되는 동안 query를 다시 pending 상태로 전환했다. 단순히 pending을 모두 초기 로딩으로 표시하면 재시도 중 오류 화면과 비활성 버튼이 사라지므로, `isFetchedAfterMount`로 최초 요청과 재시도를 구분했다.

#### 최종 결정

- 결정: 수정 후 채택
- 반영 내용: React Query 조회 hook, 앱 생명주기의 QueryClient, 로딩·오류·콘텐츠 컴포넌트, 명시적 재시도와 자동 refetch 방지 정책을 구현했다. 후속 요청에 따라 `NotFoundComponent`를 제거하고 빈 배열도 콘텐츠에 전달해 보드가 전체·컬럼 빈 상태를 처리하도록 책임을 옮겼다.
- 결정 근거: 요구사항 2.1의 조회·로딩·오류·재시도 흐름과 성공 데이터 전달을 사용자 관점 테스트로 확인했고 기존 mock API 테스트를 포함한 전체 검증을 통과했다. 전체 데이터 0건의 최종 빈 상태 표시는 후속 보드 구현에서 검증해야 한다.

---

### 지원자 단계 보드 표시

- 관련 요구사항: `2.2 단계별 보드와 지원자 카드 표시`
- 작업 상태: 완료
- 관련 커밋: 미커밋

#### 주요 프롬프트

> 먼저 채용 단계를 표시할 페이지 보드를 먼저 만들어보자 데이터는 연결하지말고 디자인만 생성할거야 내가 준 이미지를 레퍼런스로 삼고 생성해줘 title 보드 이렇게 구성하되 컨텐츠가 중앙정렬 되면 좋겠어

주요 후속 프롬프트:

- “지원자 관리면 좋을 거 같아 타이틀이 너무 크지 않게 해줘”
- “아니다 데이터 연결도 같이하자 데이터가 없을 때 처리도 같이 해줘 보드 상단에 컬럼들은 그대로 두고 하단 카드가 들어가는 영역에 지원자가 없습니다 이렇게 하면 좋을 거 같아”
- “보드만 가로 스크롤 해줘”
- “전체 지원자가 없으면 컬럼 헤더는 그대로 두고 아래 영역에 지원자가 없습니다가 한번만 보이게 해줘”

#### AI가 제안한 핵심 내용

- 성공 조회 경계인 `ContentComponent`에서 보드 전용 가로 스크롤 영역과 `지원자 관리` 제목을 제공한다.
- `ApplicantBoard`가 query 데이터를 단계별로 순수하게 분류하고, `BoardColumn`과 읽기 전용 `ApplicantCard`가 각각 컬럼과 카드 표시를 맡는다.
- 스타일은 별도 plain CSS로 두고, 전체 빈 상태와 컬럼 빈 상태를 구분한다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상          | 확인 방법 또는 근거                | 결과                                                                                                        |
| ----------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 보드 구조와 데이터 연결 | 브라우저 QA와 컴포넌트·통합 테스트 | 다섯 개 시맨틱 컬럼이 렌더되고 실제 query 결과가 보드에 전달됨                                              |
| 좁은 화면의 스크롤 범위 | 390px viewport 브라우저 QA         | `bodyScrollWidth` 390, 보드 client width 366, 보드 scroll width 1200으로 페이지가 아닌 보드만 가로 스크롤함 |
| 빈 상태                 | 보드 컴포넌트·통합 테스트          | 데이터가 있는 보드의 빈 컬럼에는 개별 메시지를, 전체 빈 보드에는 헤더 아래 메시지 하나를 표시함             |

#### AI 제안에서 발견한 문제

- 없음.

#### 최종 결정

- 결정: 수정 후 채택
- 반영 내용: 제공된 이미지는 시각적 참고로만 사용하고, `ContentComponent` → `ApplicantBoard` → `BoardColumn` → `ApplicantCard` 구성과 plain CSS로 요구된 단계 보드 표시를 구현했다.
- 결정 근거: 사용자가 제목 크기, query 데이터 연결, 단계별 건수, 보드 전용 가로 스크롤, 컬럼·전체 빈 상태의 표시 규칙을 직접 지정했다.

---

### Tailwind 기반 지원자 보드 스타일 전환

- 관련 요구사항: `2.2 단계별 보드와 지원자 카드 표시`
- 작업 상태: 완료
- 관련 커밋: 미커밋

#### 주요 프롬프트

> “개발 속도랑 일관성을 높이기 위해 tailwind 도입해줄래?”

주요 후속 프롬프트:

- “전체 전환하는데 일단 현재 보드 먼저 전환해줘”

#### AI가 제안한 핵심 내용

- plain component CSS, JSX utility, `@apply` 유지 방식의 장단점을 비교하고, 공식 Tailwind v4 Vite plugin을 이용한 utility-first 전환을 제안했다.
- 전체 UI 전환을 목표로 하되 현재 범위에서는 지원자 보드만 전환하고, 로딩·오류 컴포넌트는 후속 범위로 유지하도록 제안했다.
- 고정 단계별 색상 utility를 `applicantBoardStages.ts`에 완전한 정적 문자열로 두어 Tailwind 빌드가 감지하도록 제안했다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상          | 확인 방법 또는 근거                             | 결과                                                                                                                                        |
| ----------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 보드 스타일 전환        | 컴포넌트 JSX와 제거된 `ApplicantBoard.css` 확인 | 보드 레이아웃·컬럼·카드·빈 상태가 Tailwind utility로 표현됨                                                                                 |
| 정적 단계 색상          | `applicantBoardStages.ts` 직접 확인             | 다섯 단계의 색상 utility가 완전한 문자열로 선언됨                                                                                           |
| build 산출물            | Tailwind v4 Vite build                          | 생성 CSS에 Tailwind layer와 utility가 포함됨                                                                                                |
| 기본 화면               | 로컬 앱의 semantic tree 확인                    | `지원자 관리` 제목, 다섯 단계 컬럼·건수, 지원자 카드가 Tailwind 전환 후에도 표시됨                                                          |
| 좁은 화면의 스크롤 범위 | 390×844 viewport 브라우저 QA                    | `bodyClientWidth`와 `bodyScrollWidth`는 390으로 같고, 보드 viewport는 client width 366·scroll width 1200이며 보드 렌더 폭은 1200으로 확인됨 |

#### AI 제안에서 발견한 문제

- 없음.

#### 최종 결정

- 결정: 채택
- 반영 내용: Tailwind v4 Vite plugin과 `src/styles.css`를 도입하고, 현재 지원자 보드를 utility-first JSX로 전환했다. 전체 UI 전환은 후속 범위로 남겼다.
- 결정 근거: 사용자가 개발 속도와 UI 일관성 향상을 위해 Tailwind 도입을 요청하고, 전체 전환의 첫 범위를 현재 보드로 지정했다.

---

### 로딩·오류 화면 Tailwind 후속 전환

- 관련 요구사항: `2.1 지원자 데이터 조회와 초기 상태 처리`
- 작업 상태: 완료
- 관련 커밋: 미커밋

#### 주요 프롬프트

> “후속으로 나머지 페이지들에 대해서도 tailwind 적용해줘”

#### AI가 제안한 핵심 내용

- 로딩 화면은 중앙 정렬된 진행 표시와 안내 문구로 구성한다.
- 오류 화면은 중앙 카드, 안전한 오류 제목, 기존 재시도 버튼으로 구성한다.
- React Query 상태 분기와 재시도 계약은 유지하고 Tailwind utility와 접근성 구조만 보강한다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상 | 확인 방법 또는 근거                                    | 결과                                                                |
| -------------- | ------------------------------------------------------ | ------------------------------------------------------------------- |
| 접근성 상태    | `App.test.tsx`의 `aria-busy`와 오류 제목 테스트        | 로딩 상태와 오류 안내 구조가 명시적으로 검증됨                      |
| 재시도 동작    | 기존 성공·실패·진행 중 재시도 테스트                   | 스타일 변경 후에도 요청 횟수와 버튼 비활성 동작이 유지됨            |
| Tailwind 표현  | `LoadingComponent.tsx`, `ErrorComponent.tsx` 직접 확인 | 별도 CSS 없이 중앙 상태 UI와 focus·disabled 표현이 utility로 구성됨 |

#### AI 제안에서 발견한 문제

- 없음.

#### 최종 결정

- 결정: 채택
- 반영 내용: 기존 조회 상태 컴포넌트에 Tailwind를 적용하고 로딩의 `aria-busy`, 오류 제목, 재시도 버튼의 focus·disabled 표현을 보강했다.
- 결정 근거: 사용자가 보드 이후 나머지 페이지도 Tailwind로 전환하도록 요청했고, 기존 query 동작을 유지한 채 상태 화면의 일관성과 접근성을 높였다.

---

### 지원자 단계 드래그 앤 드롭과 저장

- 관련 요구사항: `2.3 지원자 단계 이동과 저장`
- 작업 상태: 완료
- 관련 커밋: 미커밋

#### 주요 프롬프트

> 지원자 카드 단계 변경을 드래그 앤 드롭 방식으로 개발할게
> 사용자가 화면을 봤을 때 드래그 앤 드롭 방식이 익숙하다고 느낄 거 같아
>
> 먼저 드래그 앤 드롭 라이브러리 먼저 정하자

주요 후속 프롬프트:

- “단계 간 이동만 지원해줘”
- “`@dnd-kit/react`은 키보드 이동도 지원하는거야?”
- “단계 단위 키보드 이동이 좋을 거 같아 일단 먼저 드래그 앤 드롭 먼저 구현하자”
- “드래그 할 때 컬럼 별 border 위 아래가 짤리는 이슈가 있어”

#### AI가 제안한 핵심 내용

- 정렬 없이 다섯 단계 컬럼 사이만 이동하는 현재 범위에는 `@dnd-kit/react` 0.5.x의 `DragDropProvider`, `useDraggable`, `useDroppable`을 사용한다.
- dnd-kit 타입은 `ApplicantBoard`, `BoardColumn`, `DraggableApplicantCard`의 presentation adapter 안에 두고, 단계 검증은 library-independent 순수 함수로 분리한다.
- `ContentComponent`가 주입된 Applicant API mutation을 호출하고, 저장 성공 후에만 applicant query를 무효화해 새 단계 데이터를 다시 조회한다.
- 이번 범위에는 단계 간 포인터 이동과 저장만 포함하며, 단계 단위 키보드 이동, 낙관적 업데이트·실패 피드백, 연속 이동 경쟁 상태는 후속 범위로 남긴다.
- 가로 스크롤 컨테이너가 컬럼 바깥쪽의 drop-target ring을 클리핑하므로, 강조를 컬럼 내부에 그리는 `ring-inset`으로 변경해 컬럼 크기와 보드 가로 스크롤을 유지한다.

#### 직접 리뷰·검증한 내용

| 검토·검증 대상        | 확인 방법 또는 근거                                                                                       | 결과                                                                                                                                                       |
| --------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| drag 결과 경계        | 순수 resolver와 board adapter 테스트                                                                      | 취소, source·target 누락, 없는 지원자, 잘못된 단계, 같은 단계는 무시하고 유효한 다른 단계 이동만 한 번 전달                                                |
| mutation과 UI 동기화  | 실제 QueryClient와 주입 Applicant API를 사용한 hook·Content·App 통합 테스트                               | 저장 성공 때만 query를 무효화·재조회하고, pending 중 중복 요청과 실패 후 재조회·화면 이동을 방지                                                           |
| 새 API graph 영속성   | 같은 in-memory `ApplicantStorage`와 고정 성공 behavior 위에서 API를 재생성해 전체 목록 대조               | 대상 한 명만 새 단계로 유지되고 다른 모든 지원자는 변경되지 않음                                                                                           |
| 데스크톱 포인터 drag  | 1280×720 in-app browser에서 `서류검토`의 김민지를 `면접`으로 실제 drag하고 DOM·class를 확인               | hover 중 `ring-2 ring-blue-500 ring-offset-2`, 성공 후 목적지에 한 번만 표시, 현재 단계 `면접`, 다른 네 명의 단계 유지                                     |
| 새로고침 영속성       | 성공 상태에서 실제 page reload 후 다섯 컬럼 전체를 재확인                                                 | 김민지가 `면접`에 한 번만 유지되고 나머지 네 명의 단계도 유지                                                                                              |
| 좁은 화면 스크롤·drag | 390×844에서 body·board 치수 측정, 보드를 `scrollLeft 220`까지 실제 스크롤한 뒤 김민지를 `처우협의`로 drag | body client·scroll width 390, board client width 366·scroll width 1200·`overflow-x: auto`; 이동 후 김민지가 `처우협의`에 한 번만 표시되고 현재 단계도 일치 |
| target 테두리 클리핑  | `BoardColumn` 회귀 테스트와 Tailwind class 확인                                                           | 외부 `ring-offset-2`를 제거하고 `ring-inset`을 적용해 강조 테두리가 컬럼 내부에 표시됨                                                                     |
| focused 테스트        | `npm test -- src/services/applicantApi.test.ts src/components/ContentComponent.test.tsx src/App.test.tsx` | 3개 파일, 24개 테스트 통과                                                                                                                                 |
| 전체 테스트           | `npm test -- --run`                                                                                       | 15개 파일, 89개 테스트 통과                                                                                                                                |
| 정적·production 검증  | `npm run lint`, `npm run build`, `git diff --check`                                                       | 모두 종료 코드 0                                                                                                                                           |

#### AI 제안에서 발견한 문제

- mock API의 지연은 10% 확률이고 대부분 응답이 즉시 끝나, 브라우저에서 저장 응답 전 원본 컬럼 유지 순간을 안정적으로 캡처하지 못했다. 실제 pointer drag에서 hover·성공·reload 영속성은 확인했고, pending 중 원본 위치 유지와 중복 요청 차단은 결정론적 App 통합 테스트로 별도 확인했다.
- 브라우저 QA 중 한 번의 drag 뒤 카드가 기존 단계에 유지됐고, reload 조회 오류도 한 번 발생했다. 기존 상태가 보존된 것을 확인하고 안전하게 재시도했으며, 성공 상태를 확인한 뒤에만 결과를 기록했다.
- 최초 target 강조의 `ring-offset-2`가 보드 가로 스크롤 컨테이너 밖으로 그려져 위·아래가 잘리는 문제가 사용자 확인에서 발견됐다. 원인을 overflow 경계와 외부 ring의 조합으로 좁힌 뒤 내부 ring으로 수정하고 회귀 테스트를 추가했다.

#### 최종 결정

- 결정: 수정 후 채택
- 반영 내용: `@dnd-kit/react` 기반 단계 간 포인터 drag adapter, 순수 drop 검증, API-backed mutation과 성공 후 query 재조회, 저장 중 동일 카드 차단, 내부 ring 방식의 target 강조, localStorage 영속성 검증을 구현했다. 단계 단위 키보드 이동과 `REQUIREMENTS.md` 2.4의 낙관적 업데이트·실패 복구는 구현하지 않았다.
- 결정 근거: 사용자가 단계 간 이동만 먼저 구현하고 제안된 1번 계획으로 진행하도록 요청했다. 자동 테스트와 실제 포인터·새로고침·390×844 검증에서 단계 저장, 단일 표시, 다른 지원자 불변성, 보드 전용 가로 스크롤을 확인했다.

---

## 새 기록 템플릿

### <!-- 기능명 -->

- 관련 요구사항: <!-- REQUIREMENTS.md의 항목 번호 또는 제목 -->
- 작업 상태: <!-- 예정 / 진행 중 / 완료 -->
- 관련 커밋: <!-- 커밋 해시 또는 미커밋 -->

#### 주요 프롬프트

> <!-- AI에게 실제로 요청한 주요 프롬프트를 기록한다. 주요 프롬프트가 여러 개라면 번호를 붙여 추가한다. -->

#### AI가 제안한 핵심 내용

- <!-- 구현 방향이나 결과에 영향을 준 제안만 기록한다. -->

#### 직접 리뷰·검증한 내용

| 검토·검증 대상 | 확인 방법 또는 근거                   | 결과               |
| -------------- | ------------------------------------- | ------------------ |
| <!-- 대상 -->  | <!-- 직접 수행한 리뷰나 검증 방법 --> | <!-- 확인 결과 --> |

#### AI 제안에서 발견한 문제

- <!-- 발견한 문제와 영향을 기록한다. 문제가 없다고 직접 확인한 경우에만 '없음'으로 기록한다. -->

#### 최종 결정

- 결정: <!-- 채택 / 수정 후 채택 / 기각 -->
- 반영 내용: <!-- 실제로 채택하거나 수정한 내용. 기각한 경우 반영하지 않은 내용을 기록한다. -->
- 결정 근거: <!-- 리뷰·검증 결과를 바탕으로 한 근거 -->
