# 채용 파이프라인 보드 요구사항

## 1. 목표와 범위

채용 담당자가 지원자를 채용 단계별로 확인하고 이동할 수 있는 파이프라인 보드를 제공한다.

- 채용 단계는 `서류검토`, `면접`, `처우협의`, `최종합격`, `불합격`으로 고정한다.
- 지원자는 현재 단계와 관계없이 다른 모든 단계로 이동할 수 있다.
- 데이터 조회와 변경은 제공되는 mock API 또는 로컬 mock 서버를 사용한다.
- 실제 백엔드 구현은 범위에 포함하지 않는다.
- 지원자 정보, 이력 정보, 메모의 작성·수정 기능은 범위에 포함하지 않는다.
- 단계 이동 방식 등 구체적인 UI 구현 방식은 이 문서에서 결정하지 않는다.
- 지원자 상세 보기는 모달 또는 사이드바 중 하나의 형태로 제공한다.
- mock API의 구체적인 실패 확률과 API 구조는 이 문서에서 결정하지 않는다.

## 2. 필수 구현 항목

### 2.1 지원자 데이터 조회와 초기 상태 처리

#### 목적

보드에 필요한 지원자 데이터를 불러오고 조회 결과에 맞는 상태를 사용자에게 보여준다.

#### 요구 동작

- 화면 진입 시 지원자 데이터를 조회한다.
- 조회가 진행 중일 때 로딩 상태를 표시한다.
- 조회가 성공하고 지원자가 없으면 전체 데이터가 없음을 알리는 빈 상태를 표시한다.
- 조회가 실패하면 오류 상태와 재시도 버튼을 표시한다.
- 사용자가 재시도 버튼을 누르면 지원자 데이터를 다시 조회한다.

#### 예외 / 실패 상황

- 초기 조회 실패 시 빈 보드나 이전에 존재하지 않은 데이터를 정상 결과처럼 표시하지 않는다.
- 재시도가 실패하면 오류 상태를 유지하고 다시 시도할 수 있어야 한다.

#### Acceptance Criteria

- 초기 조회가 완료되기 전에는 로딩 상태가 화면에 보인다.
- 지원자 0건이 반환되면 전체 데이터가 없다는 빈 상태가 보인다.
- 조회 실패가 발생하면 오류 안내와 재시도 버튼이 함께 보인다.
- 오류 상태에서 재시도 버튼을 한 번 누르면 새로운 조회가 한 번 수행된다.
- 재시도 조회가 성공하면 오류 상태가 사라지고 반환된 데이터가 보드에 표시된다.

#### 의존성

- 없음.

### 2.2 단계별 보드와 지원자 카드 표시

#### 목적

채용 담당자가 전체 지원자의 현재 채용 단계를 한 화면에서 확인할 수 있게 한다.

#### 요구 동작

- 다섯 개 채용 단계를 각각 구분된 컬럼으로 표시한다.
- 데이터가 없는 단계도 컬럼 자체는 표시한다.
- 각 지원자는 현재 단계에 해당하는 컬럼에 한 번만 표시한다.
- 지원자 카드에는 이름, 직무, 지원일, 현재 단계를 표시한다.

#### 예외 / 실패 상황

- 특정 단계에 지원자가 없으면 해당 컬럼이 비어 있음을 알 수 있어야 한다.
- 동일한 지원자가 두 개 이상의 컬럼에 중복 표시되어서는 안 된다.

#### Acceptance Criteria

- `서류검토`, `면접`, `처우협의`, `최종합격`, `불합격` 컬럼이 모두 보인다.
- 각 카드에서 이름, 직무, 지원일, 현재 단계를 확인할 수 있다.
- 각 카드의 현재 단계와 카드가 표시된 컬럼이 일치한다.
- 한 지원자 데이터는 보드 전체에서 정확히 한 번 표시된다.
- 지원자가 없는 컬럼은 빈 상태로 표시되고 다른 컬럼의 카드 표시에 영향을 주지 않는다.

#### 의존성

- `2.1 지원자 데이터 조회와 초기 상태 처리`가 완료되어야 한다.

### 2.3 지원자 단계 이동과 저장

#### 목적

채용 담당자가 지원자의 채용 단계를 변경하고, 변경 결과를 이후에도 유지할 수 있게 한다.

#### 요구 동작

- 사용자는 지원자 카드를 현재 단계를 제외한 다른 모든 단계로 이동할 수 있다.
- 이동이 완료되면 카드의 컬럼과 현재 단계 표시를 새 단계에 맞게 변경한다.
- 단계 변경 결과를 mock API에 저장한다.
- 저장에 성공한 이동 결과는 화면을 새로고침한 뒤에도 유지한다.
- 단계 이동의 구체적인 조작 방식은 제한하지 않는다.

#### 예외 / 실패 상황

- 저장이 완료되지 않은 변경을 영구 저장된 결과로 간주해서는 안 된다.
- 한 카드의 이동이 다른 카드의 단계에 영향을 주어서는 안 된다.

#### Acceptance Criteria

- 각 단계의 카드에서 현재 단계를 제외한 나머지 네 단계로 이동할 수 있다.
- 이동 후 카드는 목적지 컬럼에 한 번만 표시되고 기존 컬럼에서는 사라진다.
- 이동 후 카드에 표시되는 현재 단계가 목적지 컬럼과 일치한다.
- 저장 성공 후 화면을 새로고침하면 해당 카드가 이동한 단계에 표시된다.
- 카드 한 장을 이동해도 다른 카드의 컬럼과 현재 단계는 변경되지 않는다.

#### 의존성

- `2.2 단계별 보드와 지원자 카드 표시`가 완료되어야 한다.

### 2.4 낙관적 업데이트와 실패 복구

#### 목적

단계 이동 결과를 즉시 보여주면서도 저장 실패로 화면과 저장 데이터가 달라지는 문제를 방지한다.

#### 요구 동작

- 단계 이동 시 저장 응답을 기다리지 않고 화면에 목적지 단계를 먼저 반영한다.
- 저장에 성공하면 먼저 반영한 화면 상태를 유지한다.
- 저장에 실패하면 카드를 이동 직전 단계로 되돌린다.
- 저장 실패와 원상 복구 사실을 사용자가 확인할 수 있는 피드백을 표시한다.

#### 예외 / 실패 상황

- 실패한 이동 결과가 새로고침 후 다시 나타나서는 안 된다.
- 실패 복구 과정에서 카드가 사라지거나 중복 표시되어서는 안 된다.
- mock API가 이동 요청에 실패할 수 있어야 하지만, 구체적인 실패 확률은 요구하지 않는다.

#### Acceptance Criteria

- 이동 직후 저장 응답이 끝나기 전에 카드가 목적지 컬럼에 표시된다.
- 저장 성공 시 카드가 목적지 컬럼에 유지된다.
- 저장 실패 시 카드가 이동 직전 컬럼으로 돌아가고 현재 단계 표시도 복구된다.
- 저장 실패 시 사용자가 실패 사실을 알 수 있는 피드백이 보인다.
- 실패 복구 후 보드 전체에서 해당 카드는 정확히 한 번만 표시된다.
- 저장 실패 후 새로고침하면 해당 카드는 실패한 이동 이전 단계에 표시된다.

#### 의존성

- `2.3 지원자 단계 이동과 저장`이 완료되어야 한다.

### 2.5 이름 검색과 직무 필터

#### 목적

지원자가 많아도 원하는 지원자를 빠르게 찾을 수 있게 한다.

#### 요구 동작

- 지원자 이름의 일부 문자열로 검색할 수 있다.
- 영문 이름 검색은 대소문자를 구분하지 않는다.
- 직무 필터는 여러 직무를 동시에 선택할 수 있다.
- 여러 직무를 선택하면 선택한 직무 중 하나에 해당하는 지원자를 표시한다.
- 이름 검색과 직무 필터를 함께 사용하면 두 조건을 모두 만족하는 지원자만 표시한다.
- 검색어가 없고 선택한 직무가 없으면 전체 지원자를 표시한다.
- 검색 또는 필터 결과는 각 지원자의 현재 단계를 유지한 채 해당 컬럼에 표시한다.

#### 예외 / 실패 상황

- 검색과 필터 결과가 0건이면 전체 데이터가 없는 상태와 구분되는 검색 결과 빈 상태를 표시한다.
- 검색이나 필터 적용이 원본 지원자 데이터 또는 저장된 단계를 변경해서는 안 된다.

#### Acceptance Criteria

- 이름의 일부를 입력하면 해당 문자열을 포함하는 이름의 지원자만 표시된다.
- 동일한 영문 검색어를 대문자와 소문자로 입력했을 때 결과가 같다.
- 두 개 이상의 직무를 선택하면 선택된 직무 중 하나에 해당하는 지원자가 모두 표시된다.
- 이름 검색과 직무 필터를 함께 적용하면 두 조건을 모두 만족하는 지원자만 표시된다.
- 검색어와 직무 선택을 모두 제거하면 전체 지원자가 다시 표시된다.
- 일치하는 지원자가 없으면 검색 또는 필터 결과가 없다는 빈 상태가 보인다.
- 지원자 200건에서 검색어 또는 직무 필터 변경 후 200ms 이내에 올바른 결과가 표시된다.

#### 의존성

- `2.2 단계별 보드와 지원자 카드 표시`가 완료되어야 한다.
- 이동된 지원자를 최신 단계에서 검색하기 위해 `2.3 지원자 단계 이동과 저장`의 결과를 반영해야 한다.

### 2.6 지원자 상세 보기

#### 목적

보드를 벗어나지 않고 지원자의 주요 정보와 상세 정보를 확인할 수 있게 한다.

#### 요구 동작

- 사용자가 지원자 카드를 선택하면 해당 지원자의 상세 정보를 표시한다.
- 상세 정보에는 이름, 직무, 지원일, 현재 단계, 이력 정보, 메모를 표시한다.
- 상세 정보는 조회 전용으로 제공한다.
- 상세 보기를 닫고 보드로 돌아갈 수 있어야 한다.
- 상세 보기는 모달 또는 사이드바 중 하나의 형태로 제공한다.

#### 예외 / 실패 상황

- 이력 정보나 메모가 없는 경우 오류로 처리하지 않고 해당 정보가 없음을 표시한다.
- 다른 지원자의 정보가 선택한 지원자의 상세 정보로 표시되어서는 안 된다.

#### Acceptance Criteria

- 임의의 카드를 선택하면 선택한 지원자의 상세 보기가 열린다.
- 상세 보기에서 이름, 직무, 지원일, 현재 단계, 이력 정보, 메모를 확인할 수 있다.
- 이력 정보가 없는 지원자는 이력 정보가 없다는 상태로 표시된다.
- 메모가 없는 지원자는 메모가 없다는 상태로 표시된다.
- 상세 화면에서 지원자 정보, 이력 정보, 메모를 변경할 수 없다.
- 상세 보기를 닫으면 보드를 다시 확인하고 조작할 수 있다.

#### 의존성

- `2.2 단계별 보드와 지원자 카드 표시`가 완료되어야 한다.
- 단계 이동 후 최신 단계를 표시하기 위해 `2.3 지원자 단계 이동과 저장`의 결과를 반영해야 한다.

## 3. 선택 구현 항목

선택 구현 항목은 필수 구현 항목이 완료된 이후 적용한다. 구현하지 않아도 필수 구현 완료 여부에는 영향을 주지 않는다.

### 3.1 연속 단계 이동의 경쟁 상태 처리

#### 목적

같은 지원자를 빠르게 연속 이동해도 응답 순서 때문에 최종 단계가 잘못 표시되거나 저장되지 않게 한다.

#### 요구 동작

- 같은 카드를 연속 이동하면 마지막으로 선택한 단계 변경을 최신 요청으로 취급한다.
- 이전 이동의 응답이 늦게 도착해도 최신 이동 결과를 덮어쓰지 않는다.
- 최신 이동이 성공하면 사용자가 마지막으로 선택한 단계가 화면과 저장 데이터의 최종 단계가 된다.

#### 예외 / 실패 상황

- 최신 이동이 실패하면 실패 피드백을 표시하고 마지막으로 저장에 성공한 단계로 복구한다.
- 먼저 시작된 이동 요청의 늦은 성공 또는 실패 응답이 복구된 상태를 다시 변경해서는 안 된다.
- 연속 이동 중 카드가 사라지거나 여러 컬럼에 중복 표시되어서는 안 된다.

#### Acceptance Criteria

- 같은 카드를 서로 다른 단계로 연속 이동하고 응답을 역순으로 완료해도, 최신 이동이 성공했다면 마지막으로 선택한 단계가 최종 표시된다.
- 위 동작 후 새로고침해도 마지막으로 선택한 단계가 유지된다.
- 최신 이동이 실패하면 카드가 마지막으로 저장에 성공한 단계로 돌아가고 실패 피드백이 보인다.
- 모든 응답이 끝난 뒤 해당 카드는 보드 전체에서 정확히 한 번만 표시된다.

#### 의존성

- `2.4 낙관적 업데이트와 실패 복구`가 완료되어야 한다.

### 3.2 1,000건 데이터 성능

#### 목적

지원자 수가 늘어나도 검색, 필터, 스크롤 조작을 원활하게 유지한다.

#### 요구 동작

- 지원자 1,000건이 있는 상태에서도 보드를 탐색할 수 있어야 한다.
- 검색어 또는 직무 필터 변경 후 200ms 이내에 결과를 화면에 반영한다.
- 스크롤 중 사용자 입력이 장시간 차단되거나 현재 확인 중인 영역의 카드가 장시간 비어 있지 않아야 한다.
- 성능을 달성하기 위한 구체적인 구현 방식은 제한하지 않는다.

#### 예외 / 실패 상황

- 성능 최적화 때문에 검색 또는 필터 결과가 누락되어서는 안 된다.
- 빠른 스크롤 후 다른 지원자의 카드 내용이 잘못 표시되어서는 안 된다.

#### Acceptance Criteria

- 지원자 1,000건에서 검색어 변경 후 200ms 이내에 올바른 결과가 표시된다.
- 지원자 1,000건에서 직무 필터 변경 후 200ms 이내에 올바른 결과가 표시된다.
- 지원자 1,000건을 연속 스크롤하는 동안 스크롤 입력이 200ms 이상 처리되지 않는 구간이 발생하지 않는다.
- 스크롤을 멈춘 뒤 현재 영역에 표시된 각 카드의 내용이 원본 지원자 데이터와 일치한다.

#### 의존성

- `2.5 이름 검색과 직무 필터`가 완료되어야 한다.

### 3.3 최근 단계 이동 되돌리기

#### 목적

사용자가 방금 수행한 단계 이동 실수를 즉시 취소할 수 있게 한다.

#### 요구 동작

- 가장 최근에 저장에 성공한 단계 이동 한 건을 되돌릴 수 있다.
- 되돌리기를 실행하면 카드를 해당 이동 직전 단계로 이동하고 그 결과를 저장한다.
- 새로운 단계 이동이 성공하면 이전 이동 대신 새로운 이동이 되돌리기 대상이 된다.
- 되돌리기 가능 여부와 이력은 새로고침 후 유지하지 않는다.

#### 예외 / 실패 상황

- 저장에 실패해 이미 복구된 이동은 되돌리기 대상으로 취급하지 않는다.
- 되돌리기 저장이 실패하면 되돌리기 직전 단계로 복구하고 실패 피드백을 표시한다.
- 되돌릴 이동이 없을 때는 단계 변경이 발생하지 않아야 한다.

#### Acceptance Criteria

- 이동 저장 성공 후 되돌리기를 한 번 실행하면 카드가 이동 직전 단계로 돌아간다.
- 되돌리기 저장 성공 후 새로고침하면 카드가 되돌아간 단계에 표시된다.
- 두 번 이상 이동한 뒤 되돌리기를 실행하면 가장 최근 이동 한 건만 취소된다.
- 실패하여 복구된 이동 뒤에는 해당 이동을 대상으로 되돌리기를 실행할 수 없다.
- 새로고침 후에는 새로고침 전에 수행한 이동을 되돌릴 수 없다.
- 되돌리기 저장 실패 시 카드가 되돌리기 직전 단계에 남고 실패 피드백이 보인다.

#### 의존성

- `2.4 낙관적 업데이트와 실패 복구`가 완료되어야 한다.
- 연속 이동 중의 되돌리기를 지원한다면 `3.1 연속 단계 이동의 경쟁 상태 처리`가 완료되어야 한다.

### 3.4 핵심 동작 테스트

#### 목적

단계 이동과 실패 복구 등 데이터 정합성에 영향을 주는 동작을 반복 검증할 수 있게 한다.

#### 요구 동작

- 단계 이동 저장 성공 동작을 검증한다.
- 낙관적 업데이트 후 저장 실패 시 원래 단계로 복구되는 동작을 검증한다.
- 검색과 다중 직무 필터의 결합 조건을 검증한다.
- 선택 구현한 기능이 있으면 해당 기능의 주요 성공 및 실패 동작도 검증한다.

#### 예외 / 실패 상황

- 외부 상태나 실행 순서에 따라 테스트 결과가 임의로 달라져서는 안 된다.
- 실패 상황을 검증하는 테스트가 실제 mock API의 무작위 실패 발생 여부에만 의존해서는 안 된다.

#### Acceptance Criteria

- 단계 이동 저장 성공 시 목적지 단계가 유지되는 검증이 통과한다.
- 단계 이동 저장 실패 시 이동 직전 단계로 복구되는 검증이 통과한다.
- 저장 실패 후 카드가 중복되거나 사라지지 않는 검증이 통과한다.
- 이름 검색과 다중 직무 필터를 함께 적용했을 때 AND 조건으로 동작하는 검증이 통과한다.
- 선택 구현한 각 기능에 대해 이 문서의 Acceptance Criteria를 확인하는 검증이 존재하고 통과한다.

#### 의존성

- 검증 대상인 필수 또는 선택 구현 항목이 완료되어야 한다.

### 3.5 키보드 접근성

#### 목적

마우스를 사용하지 않는 사용자도 핵심 채용 관리 동작을 수행할 수 있게 한다.

#### 요구 동작

- 키보드만으로 지원자 카드에 접근할 수 있다.
- 키보드만으로 지원자를 현재 단계를 제외한 모든 단계로 이동할 수 있다.
- 키보드만으로 지원자 상세 보기를 열고 닫을 수 있다.
- 현재 키보드 초점 위치를 화면에서 확인할 수 있어야 한다.
- 단계 이동 방식이 포인터 조작을 사용하더라도 동일한 결과를 내는 키보드 경로를 제공한다.

#### 예외 / 실패 상황

- 상세 보기를 연 뒤 키보드 초점이 사용자가 접근할 수 없는 영역으로 이동해서는 안 된다.
- 단계 이동 후 키보드만으로 다음 동작을 계속할 수 있어야 한다.

#### Acceptance Criteria

- 포인터 입력 없이 키보드만 사용해 임의의 지원자 카드에 도달할 수 있다.
- 키보드만 사용해 임의의 카드를 나머지 네 단계 중 하나로 이동할 수 있다.
- 키보드만 사용해 선택한 지원자의 상세 보기를 열고 닫을 수 있다.
- 키보드로 이동하는 동안 현재 초점이 있는 요소를 시각적으로 구분할 수 있다.
- 상세 보기를 닫은 뒤 키보드로 보드 조작을 계속할 수 있다.

#### 의존성

- `2.3 지원자 단계 이동과 저장`과 `2.6 지원자 상세 보기`가 완료되어야 한다.

## 4. 구현 순서와 의존 관계 요약

1. 지원자 데이터 조회와 초기 상태 처리
2. 단계별 보드와 지원자 카드 표시
3. 지원자 단계 이동과 저장
4. 낙관적 업데이트와 실패 복구
5. 이름 검색과 직무 필터
6. 지원자 상세 보기
7. 선택 구현 항목
   - 연속 단계 이동의 경쟁 상태 처리
   - 1,000건 데이터 성능
   - 최근 단계 이동 되돌리기
   - 핵심 동작 테스트
   - 키보드 접근성
